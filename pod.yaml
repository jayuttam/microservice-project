apiVersion: v1
kind: Pod
metadata:
  name: microservice-project-pod
spec:
  containers:
    - name: microservice-project-container
      image: microservice-project:latest
      command: ["python", "app.py"]
      imagePullPolicy: IfNotPresent
